[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "accessaudit"
version = "0.1.0"
description = "Defensive access-control audit helpers"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["accessaudit"]
